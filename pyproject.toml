[project]
name = "dsa-practice"
version = "0.1.0"
description = ""
authors = [
    {name = "mahavir rajpurohit mrajpurohit1912@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy (>=2.3.4,<3.0.0)",
    "pandas (>=2.3.3,<3.0.0)",
]

[tool.poetry]
packages = [{include = "dsa_practice", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
